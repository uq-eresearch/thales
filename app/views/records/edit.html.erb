<% content_for(:page_title) do "#{@data.display_title} (editing)" end %>
<% content_for(:page_heading) do "Editing #{Thales::Datamodel::LABEL_FOR[@record.ser_type]} record" end %>

<!-- UUID: <%= @record.uuid %> -->

<%= render 'form' %>
